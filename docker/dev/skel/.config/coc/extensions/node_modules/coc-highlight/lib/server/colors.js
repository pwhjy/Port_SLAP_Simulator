"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const matchers_1 = require("./matchers");
function parseDocumentColors(document, symbols) {
    let res = [];
    symbols.forEach(o => {
        let color = matchers_1.getNameColor(o.text);
        if (color) {
            res.push({ color, range: o.range });
        }
    });
    res.push(...matchers_1.findColorHex(document));
    res.push(...matchers_1.findColorFunctions(document));
    res.push(...matchers_1.findHwb(document));
    return res;
}
exports.parseDocumentColors = parseDocumentColors;
//# sourceMappingURL=colors.js.map