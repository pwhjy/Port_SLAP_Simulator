import { OutputChannel } from 'coc.nvim';
import { UltiSnipsFile } from './types';
export default class UltiSnipsParser {
    private pyMethod;
    private channel?;
    private trace;
    constructor(pyMethod: string, channel?: OutputChannel, trace?: string);
    parseUltisnipsFile(filepath: string): Promise<Partial<UltiSnipsFile>>;
    resolveUltisnipsBody(body: string): Promise<string>;
    private execute;
    private error;
    private debug;
}
