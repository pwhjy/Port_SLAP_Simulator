/******************************************************************
MIT License http://www.opensource.org/licenses/mit-license.php
Author Qiming Zhao <chemzqm@gmail> (https://github.com/chemzqm)
*******************************************************************/
import { Document, OutputChannel } from 'coc.nvim';
import { Position, Range } from 'vscode-languageserver-types';
import BaseProvider from './baseProvider';
import { FileItem, Snippet, SnippetEdit, UltiSnipsConfig } from './types';
export declare class UltiSnippetsProvider extends BaseProvider {
    private channel;
    private trace;
    protected config: UltiSnipsConfig;
    private snippetFiles;
    private pythonCode;
    private pyMethod;
    private disposables;
    private directories;
    private parser;
    constructor(channel: OutputChannel, trace: string, config: UltiSnipsConfig);
    init(): Promise<void>;
    loadSnippetsFromFile(filetype: string, directory: string, filepath: string): Promise<void>;
    resolveSnippetBody(snippet: Snippet, range: Range, line: string): Promise<string>;
    checkContext(context: string): Promise<any>;
    getTriggerSnippets(document: Document, position: Position, autoTrigger?: boolean): Promise<SnippetEdit[]>;
    getSnippetFiles(): Promise<string[]>;
    getSnippets(): Promise<Snippet[]>;
    getAllSnippetFiles(): Promise<FileItem[]>;
    private getSnippetFileItems;
    dispose(): void;
}
