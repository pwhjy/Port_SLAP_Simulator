import { CompletionItemProvider, ProviderResult, OutputChannel } from 'coc.nvim';
import { TextDocument, Position, CancellationToken, CompletionContext, CompletionItem, Disposable } from 'vscode-languageserver-protocol';
export default class LanguageProvider implements CompletionItemProvider {
    private channel;
    private trace;
    disposables: Disposable[];
    private collection;
    constructor(channel: OutputChannel, trace?: string);
    private shouldValidate;
    private validate;
    provideCompletionItems(_document: TextDocument, position: Position, _token: CancellationToken, context: CompletionContext): ProviderResult<CompletionItem[]>;
    resolveCompletionItem(item: CompletionItem): Promise<CompletionItem>;
}
